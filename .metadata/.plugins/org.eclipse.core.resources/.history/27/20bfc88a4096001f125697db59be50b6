<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

<head>
	<title>Mutxamel Club de Fútbol</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
	<link th:href="@{/css/estilos.css}" rel="stylesheet">
	<link rel="icon" href="/images/favicon.ico" type="image/x-icon">
	<link href="https://fonts.googleapis.com/css2?family=Fredericka+the+Great&display=swap" rel="stylesheet">
</head>

<style>
	.category-header {
		background-color: #007bff;
		color: #fff;
		padding: 10px;
		margin-top: 20px;
		text-align: center;
		font-weight: bold;
	}

	/* Estilos de la cuadrícula de tarjetas */
	.team-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		/* Tres columnas iguales */
		gap: 15px;
		/* Espacio entre tarjetas */
		margin-top: 20px;
	}

	.team-card {
		border: 1px solid #ddd;
		border-radius: 8px;
		padding: 15px;
		background-color: #f8f9fa;
		text-align: center;
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
	}

	.team-card h5 {
		font-size: 1.25em;
		margin-bottom: 10px;
		color: #007bff;
	}
</style>

<body>

	<!-- Incluir el header -->
	<div th:replace="~{cabecera.html}"></div>

	<main>

		<div class="container">
			<h1 class="text-center my-4">Lista de Equipos</h1>

			<!-- Agrupación de equipos por categoría -->
			<div th:each="entry : ${equiposPorCategoria}">
				<div class="category-header">
					<!-- <h2 th:text="${entry.key}">Categoría</h2> -->
				</div>

				<!-- Cuadrícula de tarjetas -->
				<div class="team-grid">
					<!-- Tarjeta para cada equipo -->
					<div th:each="equipo : ${entry.value}">
						<a th:href="@{/admin/equipos/{id}(id=${equipo.id})}" class="team-card">
							<h5 th:text="${equipo.nombre}">Nombre del Equipo</h5>
						</a>
					</div>
				</div>
			</div>
		</div>

	</main>

	<!-- Incluir el footer -->
	<div th:replace="~{footer.html}"></div>

	<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>