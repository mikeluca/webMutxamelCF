version: "3.8"

services:
  mutxamelcf:
    image: mutxamelcf-app
    container_name: mutxamelcf
    ports:
      - "8080:8080"
    networks:
      - mutxanet
    restart: unless-stopped

networks:
  mutxanet:
    external: true
